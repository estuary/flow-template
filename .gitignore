# Test database of the catalog.
/acmeCo.db

# Directories use by nodejs builds and runtime
/dist/
/flowctl_develop/
/node_modules/

# These are generated by flowctl automatically when building catalogs.
# You may ignore these files or check them in as you wish.
# Files inside flow_generated should never be modified manually, since
# they are regenerated whenever your catalog is modified.
/flow_generated/

# These are ok to customize, since flowctl will only generate them if they are missing.
/package.json
/package-lock.json
/tsconfig.json
/.prettierrc.js
/.eslintrc.js